#include <iostream>
using namespace std;
int court(double num)
{
    int fac(char *f);
    double x;
    int a=(int)num,b;
    x=num-a;
    char numf[12];
    sprintf (numf ,"%.9g",x);
    b=fac(numf)-2;
    return(b);
}
int fac(char *f)
{
    int i,a;
    for ( a = 0; *(f+a)!='\0';a++)
    {
        int i = 1;
        i++;
    }
    return (a);
}