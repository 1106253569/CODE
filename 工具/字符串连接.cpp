#include <iostream>
using namespace std;
char *str_cat( char *s, char *t )
{
    int fac(char *f);
    int n=fac(s);
    char *g;
    g = s;
    for (int j = 0; j <= fac(t);j++)
        *(g+j+n) = *(t + j);
    return (g);
}
int fac(char *f)
{
    int i,a;
    for ( a = 0; *(f+a)!='\0';a++)
    {
        int i = 1;
        i++;
    }
    return (a);
}